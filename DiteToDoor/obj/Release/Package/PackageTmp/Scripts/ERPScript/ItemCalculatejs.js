
/// this  script to clculate total befor dis  and totla amount after dis for ever added item 
// بحساب القيمه واجمالي القيمه بعد الخصم لكل عنصر بختاره عنصر عنصر 



$(document).on("change keyup blur", "#Price", function () {
    var quantity = $('#Qty').val();
    var price = $('#Price').val();

    var mult = quantity * price;

    $('#Total').text(mult.toFixed(2));
    $("#Net").text(mult.toFixed(2));


});

$(document).on("change keyup blur", "#Qty", function () {
    var quantity = $('#Qty').val();
    var price = $('#Price').val();

    var mult = quantity * price;

    $('#Total').text(mult.toFixed(2));
    $("#Net").text(mult.toFixed(2));


});
///////////////////////////////////////////////////////////////
$(document).on("change keyup blur", "#Row1 .Price", function () {
    var quantity = $('#Row1 .Qty').val();
    var price = $('#Row1 .Price').val();

    var mult = quantity * price;

    $('#Row1 .Total').text(mult.toFixed(2));
    $("#Row1 .Net").text(mult.toFixed(2));


});

$(document).on("change keyup blur", "#Row1 .Qty", function () {
    var quantity = $('#Row1 .Qty').val();
    var price = $('#Row1 .Price').val();

    var mult = quantity * price;

    $('#Row1 .Total').text(mult.toFixed(2));
    $("#Row1 .Net").text(mult.toFixed(2));


});
///////////////////////////////////////////////////////////////
$(document).on("change keyup blur", "#Row2 .Price", function () {
    var quantity = $('#Row2 .Qty').val();
    var price = $('#Row2 .Price').val();

    var mult = quantity * price;

    $('#Row2 .Total').text(mult.toFixed(2));
    $("#Row2 .Net").text(mult.toFixed(2));


});

$(document).on("change keyup blur", "#Row2 .Qty", function () {
    var quantity = $('#Row2 .Qty').val();
    var price = $('#Row2 .Price').val();

    var mult = quantity * price;

    $('#Row2 .Total').text(mult.toFixed(2));
    $("#Row2 .Net").text(mult.toFixed(2));


});
///////////////////////////////////////////////////////////////
$(document).on("change keyup blur", "#Row3 .Price", function () {
    var quantity = $('#Row3 .Qty').val();
    var price = $('#Row3 .Price').val();

    var mult = quantity * price;

    $('#Row3 .Total').text(mult.toFixed(2));
    $("#Row3 .Net").text(mult.toFixed(2));


});

$(document).on("change keyup blur", "#Row3 .Qty", function () {
    var quantity = $('#Row3 .Qty').val();
    var price = $('#Row3 .Price').val();

    var mult = quantity * price;

    $('#Row3 .Total').text(mult.toFixed(2));
    $("#Row3 .Net").text(mult.toFixed(2));


});
///////////////////////////////////////////////////////////////
$(document).on("change keyup blur", "#Row3 .Price", function () {
    var quantity = $('#Row3 .Qty').val();
    var price = $('#Row3 .Price').val();

    var mult = quantity * price;

    $('#Row3 .Total').text(mult.toFixed(2));
    $("#Row3 .Net").text(mult.toFixed(2));


});

$(document).on("change keyup blur", "#Row3 .Qty", function () {
    var quantity = $('#Row3 .Qty').val();
    var price = $('#Row3 .Price').val();

    var mult = quantity * price;

    $('#Row3 .Total').text(mult.toFixed(2));
    $("#Row3 .Net").text(mult.toFixed(2));


});
///////////////////////////////////////////////////////////////
$(document).on("change keyup blur", "#Row4 .Price", function () {
    var quantity = $('#Row4 .Qty').val();
    var price = $('#Row4 .Price').val();
    
    var mult = quantity * price;
    
    $('#Row4 .Total').text(mult.toFixed(2));

    $("#Row4 .Net").text(mult.toFixed(2));


});

$(document).on("change keyup blur", "#Row4 .Qty", function () {
    var quantity = $('#Row4 .Qty').val();
    var price = $('#Row4 .Price').val();

    var mult = quantity * price;

    $('#Row4 .Total').text(mult.toFixed(2));
    $("#Row4 .Net").text(mult.toFixed(2));


});
///////////////////////////////////////////////////////////////
$(document).on("change keyup blur", "#Row5 .Price", function () {
    var quantity = $('#Row5 .Qty').val();
    var price = $('#Row5 .Price').val();

    var mult = quantity * price;

    $('#Row5 .Total').text(mult.toFixed(2));
    $("#Row5 .Net").text(mult.toFixed(2));


});

$(document).on("change keyup blur", "#Row5 .Qty", function () {
    var quantity = $('#Row5 .Qty').val();
    var price = $('#Row5 .Price').val();

    var mult = quantity * price;

    $('#Row5 .Total').text(mult.toFixed(2));
    $("#Row5 .Net").text(mult.toFixed(2));


});
///////////////////////////////////////////////////////////////
$(document).on("change keyup blur", "#Row6 .Price", function () {
    var quantity = $('#Row6 .Qty').val();
    var price = $('#Row6 .Price').val();

    var mult = quantity * price;

    $('#Row6 .Total').text(mult.toFixed(2));
    $("#Row6 .Net").text(mult.toFixed(2));


});

$(document).on("change keyup blur", "#Row6 .Qty", function () {
    var quantity = $('#Row6 .Qty').val();
    var price = $('#Row6 .Price').val();

    var mult = quantity * price;

    $('#Row6 .Total').text(mult.toFixed(2));
    $("#Row6 .Net").text(mult.toFixed(2));


});
///////////////////////////////////////////////////////////////
$(document).on("change keyup blur", "#Row7 .Price", function () {
    var quantity = $('#Row7 .Qty').val();
    var price = $('#Row7 .Price').val();

    var mult = quantity * price;

    $('#Row7 .Total').text(mult.toFixed(2));
    $("#Row7 .Net").text(mult.toFixed(2));


});

$(document).on("change keyup blur", "#Row7 .Qty", function () {
    var quantity = $('#Row7 .Qty').val();
    var price = $('#Row7 .Price').val();

    var mult = quantity * price;

    $('#Row7 .Total').text(mult.toFixed(2));
    $("#Row7 .Net").text(mult.toFixed(2));


});
///////////////////////////////////////////////////////////////
$(document).on("change keyup blur", "#Row8 .Price", function () {
    var quantity = $('#Row8 .Qty').val();
    var price = $('#Row8 .Price').val();

    var mult = quantity * price;

    $('#Row8 .Total').text(mult.toFixed(2));
    $("#Row8 .Net").text(mult.toFixed(2));


});

$(document).on("change keyup blur", "#Row8 .Qty", function () {
    var quantity = $('#Row8 .Qty').val();
    var price = $('#Row8 .Price').val();

    var mult = quantity * price;

    $('#Row8 .Total').text(mult.toFixed(2));
    $("#Row8 .Net").text(mult.toFixed(2));


});
///////////////////////////////////////////////////////////////
$(document).on("change keyup blur", "#Row9 .Price", function () {
    var quantity = $('#Row9 .Qty').val();
    var price = $('#Row9 .Price').val();

    var mult = quantity * price;

    $('#Row9 .Total').text(mult.toFixed(2));
    $("#Row9 .Net").text(mult.toFixed(2));


});

$(document).on("change keyup blur", "#Row9 .Qty", function () {
    var quantity = $('#Row9 .Qty').val();
    var price = $('#Row9 .Price').val();

    var mult = quantity * price;

    $('#Row9 .Total').text(mult.toFixed(2));
    $("#Row9 .Net").text(mult.toFixed(2));


});
///////////////////////////////////////////////////////////////
$(document).on("change keyup blur", "#Row10 .Price", function () {
    var quantity = $('#Row10 .Qty').val();
    var price = $('#Row10 .Price').val();

    var mult = quantity * price;

    $('#Row10 .Total').text(mult.toFixed(2));
    $("#Row10 .Net").text(mult.toFixed(2));


});

$(document).on("change keyup blur", "#Row10 .Qty", function () {
    var quantity = $('#Row10 .Qty').val();
    var price = $('#Row10 .Price').val();

    var mult = quantity * price;

    $('#Row10 .Total').text(mult.toFixed(2));
    $("#Row10 .Net").text(mult.toFixed(2));


});
///////////////////////////////////////////////////////////////
$(document).on("change keyup blur", "#Row11 .Price", function () {
    var quantity = $('#Row11 .Qty').val();
    var price = $('#Row1 .Price').val();

    var mult = quantity * price;

    $('#Row11 .Total').text(mult.toFixed(2));
    $("#Row11 .Net").text(mult.toFixed(2));


});

$(document).on("change keyup blur", "#Row11 .Qty", function () {
    var quantity = $('#Row11 .Qty').val();
    var price = $('#Row11 .Price').val();

    var mult = quantity * price;

    $('#Row11 .Total').text(mult.toFixed(2));
    $("#Row11 .Net").text(mult.toFixed(2));


});
///////////////////////////////////////////////////////////////
$(document).on("change keyup blur", "#Row12 .Price", function () {
    var quantity = $('#Row12 .Qty').val();
    var price = $('#Row12 .Price').val();

    var mult = quantity * price;

    $('#Row12 .Total').text(mult.toFixed(2));
    $("#Row12 .Net").text(mult.toFixed(2));


});

$(document).on("change keyup blur", "#Row12 .Qty", function () {
    var quantity = $('#Row12 .Qty').val();
    var price = $('#Row12 .Price').val();

    var mult = quantity * price;

    $('#Row12 .Total').text(mult.toFixed(2));
    $("#Row12 .Net").text(mult.toFixed(2));


});
///////////////////////////////////////////////////////////////
$(document).on("change keyup blur", "#Row13 .Price", function () {
    var quantity = $('#Row13 .Qty').val();
    var price = $('#Row13 .Price').val();

    var mult = quantity * price;

    $('#Row13 .Total').text(mult.toFixed(2));
    $("#Row13 .Net").text(mult.toFixed(2));


});

$(document).on("change keyup blur", "#Row13 .Qty", function () {
    var quantity = $('#Row13 .Qty').val();
    var price = $('#Row13 .Price').val();

    var mult = quantity * price;

    $('#Row13 .Total').text(mult.toFixed(2));
    $("#Row13 .Net").text(mult.toFixed(2));


});
///////////////////////////////////////////////////////////////
$(document).on("change keyup blur", "#Row14 .Price", function () {
    var quantity = $('#Row14 .Qty').val();
    var price = $('#Row14 .Price').val();

    var mult = quantity * price;

    $('#Row14 .Total').text(mult.toFixed(2));
    $("#Row14 .Net").text(mult.toFixed(2));


});

$(document).on("change keyup blur", "#Row14 .Qty", function () {
    var quantity = $('#Row14 .Qty').val();
    var price = $('#Row14 .Price').val();

    var mult = quantity * price;

    $('#Row14 .Total').text(mult.toFixed(2));
    $("#Row14 .Net").text(mult.toFixed(2));


});
///////////////////////////////////////////////////////////////
$(document).on("change keyup blur", "#Row15 .Price", function () {
    var quantity = $('#Row15 .Qty').val();
    var price = $('#Row15 .Price').val();

    var mult = quantity * price;

    $('#Row15 .Total').text(mult.toFixed(2));
    $("#Row15 .Net").text(mult.toFixed(2));


});
$(document).on("change keyup blur", "#Row15 .Qty", function () {
    var quantity = $('#Row15 .Qty').val();
    var price = $('#Row15 .Price').val();

    var mult = quantity * price;

    $('#Row15 .Total').text(mult.toFixed(2));
    $("#Row15 .Net").text(mult.toFixed(2));


});
///////////////////////////////////////////////////////////////
$(document).on("change keyup blur", "#Row16 .Price", function () {
    var quantity = $('#Row16 .Qty').val();
    var price = $('#Row16 .Price').val();

    var mult = quantity * price;

    $('#Row16 .Total').text(mult.toFixed(2));
    $("#Row16 .Net").text(mult.toFixed(2));


});

$(document).on("change keyup blur", "#Row16 .Qty", function () {
    var quantity = $('#Row16 .Qty').val();
    var price = $('#Row16 .Price').val();

    var mult = quantity * price;

    $('#Row16 .Total').text(mult.toFixed(2));
    $("#Row16 .Net").text(mult.toFixed(2));


});
///////////////////////////////////////////////////////////////
$(document).on("change keyup blur", "#Row17 .Price", function () {
    var quantity = $('#Row17 .Qty').val();
    var price = $('#Row17 .Price').val();

    var mult = quantity * price;

    $('#Row17 .Total').text(mult.toFixed(2));
    $("#Row17 .Net").text(mult.toFixed(2));


});

$(document).on("change keyup blur", "#Row17 .Qty", function () {
    var quantity = $('#Row17 .Qty').val();
    var price = $('#Row17 .Price').val();

    var mult = quantity * price;

    $('#Row17 .Total').text(mult.toFixed(2));
    $("#Row17 .Net").text(mult.toFixed(2));


});
$(document).on("change keyup blur", "#Row18 .Price", function () {
    var quantity = $('#Row18 .Qty').val();
    var price = $('#Row18 .Price').val();

    var mult = quantity * price;

    $('#Row18 .Total').text(mult.toFixed(2));
    $("#Row18 .Net").text(mult.toFixed(2));


});

$(document).on("change keyup blur", "#Row18 .Qty", function () {
    var quantity = $('#Row18 .Qty').val();
    var price = $('#Row18 .Price').val();

    var mult = quantity * price;

    $('#Row18 .Total').text(mult.toFixed(2));
    $("#Row18 .Net").text(mult.toFixed(2));


});
$(document).on("change keyup blur", "#Row19 .Price", function () {
    var quantity = $('#Row19 .Qty').val();
    var price = $('#Row19 .Price').val();

    var mult = quantity * price;

    $('#Row19 .Total').text(mult.toFixed(2));
    $("#Row19 .Net").text(mult.toFixed(2));


});

$(document).on("change keyup blur", "#Row19 .Qty", function () {
    var quantity = $('#Row19 .Qty').val();
    var price = $('#Row19 .Price').val();

    var mult = quantity * price;

    $('#Row19 .Total').text(mult.toFixed(2));
    $("#Row19 .Net").text(mult.toFixed(2));


});
///////////////////////////////////////////////////////////////
$(document).on("change keyup blur", "#Row20 .Price", function () {
    var quantity = $('#Row20 .Qty').val();
    var price = $('#Row20 .Price').val();

    var mult = quantity * price;

    $('#Row20 .Total').text(mult.toFixed(2));
    $("#Row20 .Net").text(mult.toFixed(2));


});

$(document).on("change keyup blur", "#Row20 .Qty", function () {
    var quantity = $('#Row20 .Qty').val();
    var price = $('#Row20 .Price').val();

    var mult = quantity * price;

    $('#Row20 .Total').text(mult.toFixed(2));
    $("#Row20 .Net").text(mult.toFixed(2));


});
///////////////////////////////////////////////////////////////
$(document).on("change keyup blur", "#Row21 .Price", function () {
    var quantity = $('#Row21 .Qty').val();
    var price = $('#Row21 .Price').val();

    var mult = quantity * price;

    $('#Row21 .Total').text(mult.toFixed(2));
    $("#Row21 .Net").text(mult.toFixed(2));


});

$(document).on("change keyup blur", "#Row21 .Qty", function () {
    var quantity = $('#Row21 .Qty').val();
    var price = $('#Row21 .Price').val();

    var mult = quantity * price;

    $('#Row21 .Total').text(mult.toFixed(2));
    $("#Row21 .Net").text(mult.toFixed(2));


});

///////////////////////////////////////////////////////////////
$(document).on("change keyup blur", "#Row22 .Price", function () {
    var quantity = $('#Row22 .Qty').val();
    var price = $('#Row22 .Price').val();

    var mult = quantity * price;

    $('#Row22 .Total').text(mult.toFixed(2));
    $("#Row22 .Net").text(mult.toFixed(2));


});

$(document).on("change keyup blur", "#Row22 .Qty", function () {
    var quantity = $('#Row22 .Qty').val();
    var price = $('#Row22 .Price').val();

    var mult = quantity * price;

    $('#Row22 .Total').text(mult.toFixed(2));
    $("#Row22 .Net").text(mult.toFixed(2));


});

///////////////////////////////////////////////////////////////
$(document).on("change keyup blur", "#Row23 .Price", function () {
    var quantity = $('#Row23 .Qty').val();
    var price = $('#Row23 .Price').val();

    var mult = quantity * price;

    $('#Row23 .Total').text(mult.toFixed(2));
    $("#Row23 .Net").text(mult.toFixed(2));


});

$(document).on("change keyup blur", "#Row23 .Qty", function () {
    var quantity = $('#Row23 .Qty').val();
    var price = $('#Row23 .Price').val();

    var mult = quantity * price;

    $('#Row23 .Total').text(mult.toFixed(2));
    $("#Row23 .Net").text(mult.toFixed(2));


});

///////////////////////////////////////////////////////////////
$(document).on("change keyup blur", "#Row24 .Price", function () {
    var quantity = $('#Row24 .Qty').val();
    var price = $('#Row24 .Price').val();

    var mult = quantity * price;

    $('#Row24 .Total').text(mult.toFixed(2));
    $("#Row24 .Net").text(mult.toFixed(2));


});

$(document).on("change keyup blur", "#Row24 .Qty", function () {
    var quantity = $('#Row24 .Qty').val();
    var price = $('#Row24 .Price').val();

    var mult = quantity * price;

    $('#Row24 .Total').text(mult.toFixed(2));
    $("#Row24 .Net").text(mult.toFixed(2));


});
///////////////////////////////////////////////////////////////
$(document).on("change keyup blur", "#Row25 .Price", function () {
    var quantity = $('#Row25 .Qty').val();
    var price = $('#Row25 .Price').val();

    var mult = quantity * price;

    $('#Row25 .Total').text(mult.toFixed(2));
    $("#Row25 .Net").text(mult.toFixed(2));


});

$(document).on("change keyup blur", "#Row25 .Qty", function () {
    var quantity = $('#Row25 .Qty').val();
    var price = $('#Row25 .Price').val();

    var mult = quantity * price;

    $('#Row25 .Total').text(mult.toFixed(2));
    $("#Row25 .Net").text(mult.toFixed(2));


});

///////////////////////////////////////////////////////////////
$(document).on("change keyup blur", "#Row26 .Price", function () {
    var quantity = $('#Row26 .Qty').val();
    var price = $('#Row26 .Price').val();

    var mult = quantity * price;

    $('#Row26 .Total').text(mult.toFixed(2));
    $("#Row26 .Net").text(mult.toFixed(2));


});

$(document).on("change keyup blur", "#Row26 .Qty", function () {
    var quantity = $('#Row26 .Qty').val();
    var price = $('#Row26 .Price').val();

    var mult = quantity * price;

    $('#Row26 .Total').text(mult.toFixed(2));
    $("#Row26 .Net").text(mult.toFixed(2));


});
///////////////////////////////////////////////////////////////
$(document).on("change keyup blur", "#Row27 .Price", function () {
    var quantity = $('#Row27 .Qty').val();
    var price = $('#Row27 .Price').val();

    var mult = quantity * price;

    $('#Row27 .Total').text(mult.toFixed(2));
    $("#Row27 .Net").text(mult.toFixed(2));


});

$(document).on("change keyup blur", "#Row27 .Qty", function () {
    var quantity = $('#Row27 .Qty').val();
    var price = $('#Row27 .Price').val();

    var mult = quantity * price;

    $('#Row27 .Total').text(mult.toFixed(2));
    $("#Row27 .Net").text(mult.toFixed(2));


});

///////////////////////////////////////////////////////////////
$(document).on("change keyup blur", "#Row28 .Price", function () {
    var quantity = $('#Row28 .Qty').val();
    var price = $('#Row28 .Price').val();

    var mult = quantity * price;

    $('#Row28 .Total').text(mult.toFixed(2));
    $("#Row28 .Net").text(mult.toFixed(2));


});

$(document).on("change keyup blur", "#Row28 .Qty", function () {
    var quantity = $('#Row28 .Qty').val();
    var price = $('#Row28 .Price').val();

    var mult = quantity * price;

    $('#Row28 .Total').text(mult.toFixed(2));
    $("#Row28 .Net").text(mult.toFixed(2));


});
///////////////////////////////////////////////////////////////
$(document).on("change keyup blur", "#Row29 .Price", function () {
    var quantity = $('#Row29 .Qty').val();
    var price = $('#Row29 .Price').val();

    var mult = quantity * price;

    $('#Row29 .Total').text(mult.toFixed(2));
    $("#Row29 .Net").text(mult.toFixed(2));


});

$(document).on("change keyup blur", "#Row29 .Qty", function () {
    var quantity = $('#Row29 .Qty').val();
    var price = $('#Row29 .Price').val();

    var mult = quantity * price;

    $('#Row29 .Total').text(mult.toFixed(2));
    $("#Row29 .Net").text(mult.toFixed(2));


});
///////////////////////////////////////////////////////////////
$(document).on("change keyup blur", "#Row30 .Price", function () {
    var quantity = $('#Row30 .Qty').val();
    var price = $('#Row30 .Price').val();

    var mult = quantity * price;

    $('#Row30 .Total').text(mult.toFixed(2));
    $("#Row30 .Net").text(mult.toFixed(2));


});

$(document).on("change keyup blur", "#Row30 .Qty", function () {
    var quantity = $('#Row30 .Qty').val();
    var price = $('#Row30 .Price').val();

    var mult = quantity * price;

    $('#Row30 .Total').text(mult.toFixed(2));
    $("#Row30 .Net").text(mult.toFixed(2));


});
//  بطريقه نسبه الخصم حساب قيه الخصم لكل صنف
$(document).on("keyup", "#Disper", function () {

    var Amount = $("#Qty").val() * $("#Price").val() - ($("#Qty").val() * $("#Price").val() * ($("#Disper").val() / 100));
    var Net = $("#Net").text()
    var DisValue = Net - Amount;
    alert(Amount);
    alert(Net);
    alert(DisValue);
    $("#DisValue").val(DisValue.toFixed(2));//قيمه الخصم نفسه
    $("#Net").text(Amount.toFixed(2));//القيمه بعد الخصم 

});
//////////////////////////////////////////////////////////////////////////////////////////////
$(document).on("keyup", "#Row1 .Disper", function () {

    var Amount = $("#Row1 .Qty").val() * $("#Row1 .Price").val() - ($("#Row1 .Qty").val() * $("#Row1 .Price").val() * ($("#Row1 .Disper").val() / 100));
    var Net = $("#Row1 .Net").text()
    var DisValue = Net - Amount;
   
    $("#Row1 .DisValue").val(DisValue.toFixed(2));//قيمه الخصم نفسه
    $("#Row1 .Net").text(Amount.toFixed(2));//القيمه بعد الخصم 

});
//////////////////////////////////////////////////////////////////////////////////////////////
$(document).on("keyup", "#Row2 .Disper", function () {

    var Amount = $("#Row2 .Qty").val() * $("#Row2 .Price").val() - ($("#Row2 .Qty").val() * $("#Row2 .Price").val() * ($("#Row2 .Disper").val() / 100));
    var Net = $("#Row2 .Net").text()
    var DisValue = Net - Amount;

    $("#Row2 .DisValue").val(DisValue.toFixed(2));//قيمه الخصم نفسه
    $("#Row2 .Net").text(Amount.toFixed(2));//القيمه بعد الخصم

});
//////////////////////////////////////////////////////////////////////////////////////////////
$(document).on("keyup", "#Row3 .Disper", function () {

    var Amount = $("#Row3 .Qty").val() * $("#Row3 .Price").val() - ($("#Row3 .Qty").val() * $("#Row3 .Price").val() * ($("#Row3 .Disper").val() / 100));
    var Net = $("#Row3 .Net").text()
    var DisValue = Net - Amount;

    $("#Row3 .DisValue").val(DisValue.toFixed(2));//قيمه الخصم نفسه
    $("#Row3 .Net").text(Amount.toFixed(2));//القيمه بعد الخصم

});
//////////////////////////////////////////////////////////////////////////////////////////////
$(document).on("keyup", "#Row4 .Disper", function () {

    var Amount = $("#Row4 .Qty").val() * $("#Row4 .Price").val() - ($("#Row4 .Qty").val() * $("#Row4 .Price").val() * ($("#Row4 .Disper").val() / 100));
    var Net = $("#Row4 .Net").text()
    var DisValue = Net - Amount;

    $("#Row4 .DisValue").val(DisValue.toFixed(2));//قيمه الخصم نفسه
    $("#Row4 .Net").text(Amount.toFixed(2));//القيمه بعد الخصم

});
//////////////////////////////////////////////////////////////////////////////////////////////
$(document).on("keyup", "#Row5 .Disper", function () {

    var Amount = $("#Row5 .Qty").val() * $("#Row5 .Price").val() - ($("#Row5 .Qty").val() * $("#Row5 .Price").val() * ($("#Row5 .Disper").val() / 100));
    var Net = $("#Row5 .Net").text()
    var DisValue = Net - Amount;

    $("#Row5 .DisValue").val(DisValue.toFixed(2));//قيمه الخصم نفسه
    $("#Row5 .Net").text(Amount.toFixed(2));//القيمه بعد الخصم

});
//////////////////////////////////////////////////////////////////////////////////////////////
$(document).on("keyup", "#Row6 .Disper", function () {

    var Amount = $("#Row6 .Qty").val() * $("#Row6 .Price").val() - ($("#Row6 .Qty").val() * $("#Row6 .Price").val() * ($("#Row6 .Disper").val() / 100));
    var Net = $("#Row6 .Net").text()
    var DisValue = Net - Amount;

    $("#Row6 .DisValue").val(DisValue.toFixed(2));//قيمه الخصم نفسه
    $("#Row6 .Net").text(Amount.toFixed(2));//القيمه بعد الخصم

});
//////////////////////////////////////////////////////////////////////////////////////////////
$(document).on("keyup", "#Row7 .Disper", function () {

    var Amount = $("#Row7 .Qty").val() * $("#Row7 .Price").val() - ($("#Row7 .Qty").val() * $("#Row7 .Price").val() * ($("#Row7 .Disper").val() / 100));
    var Net = $("#Row7 .Net").text()
    var DisValue = Net - Amount;

    $("#Row7 .DisValue").val(DisValue.toFixed(2));//قيمه الخصم نفسه
    $("#Row7 .Net").text(Amount.toFixed(2));//القيمه بعد الخصم

});
//////////////////////////////////////////////////////////////////////////////////////////////
$(document).on("keyup", "#Row8 .Disper", function () {

    var Amount = $("#Row8 .Qty").val() * $("#Row8 .Price").val() - ($("#Row8 .Qty").val() * $("#Row8 .Price").val() * ($("#Row8 .Disper").val() / 100));
    var Net = $("#Row8 .Net").text()
    var DisValue = Net - Amount;

    $("#Row8 .DisValue").val(DisValue.toFixed(2));//قيمه الخصم نفسه
    $("#Row8 .Net").text(Amount.toFixed(2));//القيمه بعد الخصم

});
//////////////////////////////////////////////////////////////////////////////////////////////
$(document).on("keyup", "#Row9 .Disper", function () {

    var Amount = $("#Row9 .Qty").val() * $("#Row9 .Price").val() - ($("#Row9 .Qty").val() * $("#Row9 .Price").val() * ($("#Row9 .Disper").val() / 100));
    var Net = $("#Row9 .Net").text()
    var DisValue = Net - Amount;

    $("#Row9 .DisValue").val(DisValue.toFixed(2));//قيمه الخصم نفسه
    $("#Row9 .Net").text(Amount.toFixed(2));//القيمه بعد الخصم

});
//////////////////////////////////////////////////////////////////////////////////////////////
$(document).on("keyup", "#Row10 .Disper", function () {

    var Amount = $("#Row10 .Qty").val() * $("#Row10 .Price").val() - ($("#Row10 .Qty").val() * $("#Row10 .Price").val() * ($("#Row10 .Disper").val() / 100));
    var Net = $("#Row10 .Net").text()
    var DisValue = Net - Amount;

    $("#Row10 .DisValue").val(DisValue.toFixed(2));//قيمه الخصم نفسه
    $("#Row10 .Net").text(Amount.toFixed(2));//القيمه بعد الخصم

});
//////////////////////////////////////////////////////////////////////////////////////////////
$(document).on("keyup", "#Row11 .Disper", function () {

    var Amount = $("#Row11 .Qty").val() * $("#Row11 .Price").val() - ($("#Row11 .Qty").val() * $("#Row11 .Price").val() * ($("#Row11 .Disper").val() / 100));
    var Net = $("#Row11 .Net").text()
    var DisValue = Net - Amount;

    $("#Row11 .DisValue").val(DisValue.toFixed(2));//قيمه الخصم نفسه
    $("#Row11 .Net").text(Amount.toFixed(2));//القيمه بعد الخصم

});
//////////////////////////////////////////////////////////////////////////////////////////////
$(document).on("keyup", "#Row12 .Disper", function () {

    var Amount = $("#Row12 .Qty").val() * $("#Row12 .Price").val() - ($("#Row12 .Qty").val() * $("#Row12 .Price").val() * ($("#Row12 .Disper").val() / 100));
    var Net = $("#Row12 .Net").text()
    var DisValue = Net - Amount;

    $("#Row12 .DisValue").val(DisValue.toFixed(2));//قيمه الخصم نفسه
    $("#Row12 .Net").text(Amount.toFixed(2));//القيمه بعد الخصم

});
//////////////////////////////////////////////////////////////////////////////////////////////
$(document).on("keyup", "#Row13 .Disper", function () {

    var Amount = $("#Row13 .Qty").val() * $("#Row13 .Price").val() - ($("#Row13 .Qty").val() * $("#Row13 .Price").val() * ($("#Row13 .Disper").val() / 100));
    var Net = $("#Row13 .Net").text()
    var DisValue = Net - Amount;

    $("#Row13 .DisValue").val(DisValue.toFixed(2));//قيمه الخصم نفسه
    $("#Row13 .Net").text(Amount.toFixed(2));//القيمه بعد الخصم

});
//////////////////////////////////////////////////////////////////////////////////////////////
$(document).on("keyup", "#Row14 .Disper", function () {

    var Amount = $("#Row14 .Qty").val() * $("#Row14 .Price").val() - ($("#Row14 .Qty").val() * $("#Row14 .Price").val() * ($("#Row14 .Disper").val() / 100));
    var Net = $("#Row14 .Net").text()
    var DisValue = Net - Amount;

    $("#Row14 .DisValue").val(DisValue.toFixed(2));//قيمه الخصم نفسه
    $("#Row14 .Net").text(Amount.toFixed(2));//القيمه بعد الخصم

});
//////////////////////////////////////////////////////////////////////////////////////////////
$(document).on("keyup", "#Row15 .Disper", function () {

    var Amount = $("#Row15 .Qty").val() * $("#Row15 .Price").val() - ($("#Row15 .Qty").val() * $("#Row15 .Price").val() * ($("#Row15 .Disper").val() / 100));
    var Net = $("#Row15 .Net").text()
    var DisValue = Net - Amount;

    $("#Row15 .DisValue").val(DisValue.toFixed(2));//قيمه الخصم نفسه
    $("#Row15 .Net").text(Amount.toFixed(2));//القيمه بعد الخصم

});
//////////////////////////////////////////////////////////////////////////////////////////////
$(document).on("keyup", "#Row16 .Disper", function () {

    var Amount = $("#Row16 .Qty").val() * $("#Row16 .Price").val() - ($("#Row16 .Qty").val() * $("#Row16 .Price").val() * ($("#Row16 .Disper").val() / 100));
    var Net = $("#Row16 .Net").text()
    var DisValue = Net - Amount;

    $("#Row16 .DisValue").val(DisValue.toFixed(2));//قيمه الخصم نفسه
    $("#Row16 .Net").text(Amount.toFixed(2));//القيمه بعد الخصم

});
//////////////////////////////////////////////////////////////////////////////////////////////
$(document).on("keyup", "#Row17 .Disper", function () {

    var Amount = $("#Row17 .Qty").val() * $("#Row17 .Price").val() - ($("#Row17 .Qty").val() * $("#Row17 .Price").val() * ($("#Row17 .Disper").val() / 100));
    var Net = $("#Row17 .Net").text()
    var DisValue = Net - Amount;

    $("#Row17 .DisValue").val(DisValue.toFixed(2));//قيمه الخصم نفسه
    $("#Row17 .Net").text(Amount.toFixed(2));//القيمه بعد الخصم

});
//////////////////////////////////////////////////////////////////////////////////////////////
$(document).on("keyup", "#Row18 .Disper", function () {

    var Amount = $("#Row18 .Qty").val() * $("#Row18 .Price").val() - ($("#Row18 .Qty").val() * $("#Row18 .Price").val() * ($("#Row18 .Disper").val() / 100));
    var Net = $("#Row18 .Net").text()
    var DisValue = Net - Amount;

    $("#Row18 .DisValue").val(DisValue.toFixed(2));//قيمه الخصم نفسه
    $("#Row18 .Net").text(Amount.toFixed(2));//القيمه بعد الخصم

});

//////////////////////////////////////////////////////////////////////////////////////////////
$(document).on("keyup", "#Row19 .Disper", function () {

    var Amount = $("#Row19 .Qty").val() * $("#Row19 .Price").val() - ($("#Row19 .Qty").val() * $("#Row19 .Price").val() * ($("#Row19 .Disper").val() / 100));
    var Net = $("#Row19 .Net").text()
    var DisValue = Net - Amount;

    $("#Row19 .DisValue").val(DisValue.toFixed(2));//قيمه الخصم نفسه
    $("#Row19 .Net").text(Amount.toFixed(2));//القيمه بعد الخصم

});
//////////////////////////////////////////////////////////////////////////////////////////////
$(document).on("keyup", "#Row20 .Disper", function () {

    var Amount = $("#Row20 .Qty").val() * $("#Row20 .Price").val() - ($("#Row20 .Qty").val() * $("#Row20 .Price").val() * ($("#Row20 .Disper").val() / 100));
    var Net = $("#Row20 .Net").text()
    var DisValue = Net - Amount;

    $("#Row20 .DisValue").val(DisValue.toFixed(2));//قيمه الخصم نفسه
    $("#Row20 .Net").text(Amount.toFixed(2));//القيمه بعد الخصم

});
//////////////////////////////////////////////////////////////////////////////////////////////
$(document).on("keyup", "#Row21 .Disper", function () {

    var Amount = $("#Row21 .Qty").val() * $("#Row21 .Price").val() - ($("#Row21 .Qty").val() * $("#Row21 .Price").val() * ($("#Row21 .Disper").val() / 100));
    var Net = $("#Row21 .Net").text()
    var DisValue = Net - Amount;

    $("#Row21 .DisValue").val(DisValue.toFixed(2));//قيمه الخصم نفسه
    $("#Row21 .Net").text(Amount.toFixed(2));//القيمه بعد الخصم

});
//////////////////////////////////////////////////////////////////////////////////////////////
$(document).on("keyup", "#Row22 .Disper", function () {

    var Amount = $("#Row22 .Qty").val() * $("#Row22 .Price").val() - ($("#Row22 .Qty").val() * $("#Row22 .Price").val() * ($("#Row22 .Disper").val() / 100));
    var Net = $("#Row22 .Net").text()
    var DisValue = Net - Amount;

    $("#Row22 .DisValue").val(DisValue.toFixed(2));//قيمه الخصم نفسه
    $("#Row22 .Net").text(Amount.toFixed(2));//القيمه بعد الخصم

});
//////////////////////////////////////////////////////////////////////////////////////////////
$(document).on("keyup", "#Row23 .Disper", function () {

    var Amount = $("#Row23 .Qty").val() * $("#Row23 .Price").val() - ($("#Row23 .Qty").val() * $("#Row23 .Price").val() * ($("#Row23 .Disper").val() / 100));
    var Net = $("#Row23 .Net").text()
    var DisValue = Net - Amount;

    $("#Row23 .DisValue").val(DisValue.toFixed(2));//قيمه الخصم نفسه
    $("#Row23 .Net").text(Amount.toFixed(2));//القيمه بعد الخصم

});
//////////////////////////////////////////////////////////////////////////////////////////////
$(document).on("keyup", "#Row24 .Disper", function () {

    var Amount = $("#Row24 .Qty").val() * $("#Row24 .Price").val() - ($("#Row24 .Qty").val() * $("#Row24 .Price").val() * ($("#Row24 .Disper").val() / 100));
    var Net = $("#Row24 .Net").text()
    var DisValue = Net - Amount;

    $("#Row24 .DisValue").val(DisValue.toFixed(2));//قيمه الخصم نفسه
    $("#Row24 .Net").text(Amount.toFixed(2));//القيمه بعد الخصم

});

//////////////////////////////////////////////////////////////////////////////////////////////
$(document).on("keyup", "#Row25 .Disper", function () {

    var Amount = $("#Row25 .Qty").val() * $("#Row25 .Price").val() - ($("#Row25 .Qty").val() * $("#Row25 .Price").val() * ($("#Row25 .Disper").val() / 100));
    var Net = $("#Row25 .Net").text()
    var DisValue = Net - Amount;

    $("#Row25 .DisValue").val(DisValue.toFixed(2));//قيمه الخصم نفسه
    $("#Row25 .Net").text(Amount.toFixed(2));//القيمه بعد الخصم

});
//////////////////////////////////////////////////////////////////////////////////////////////
$(document).on("keyup", "#Row26 .Disper", function () {

    var Amount = $("#Row26 .Qty").val() * $("#Row26 .Price").val() - ($("#Row26 .Qty").val() * $("#Row26 .Price").val() * ($("#Row26 .Disper").val() / 100));
    var Net = $("#Row26 .Net").text()
    var DisValue = Net - Amount;

    $("#Row26 .DisValue").val(DisValue.toFixed(2));//قيمه الخصم نفسه
    $("#Row26 .Net").text(Amount.toFixed(2));//القيمه بعد الخصم

});
//////////////////////////////////////////////////////////////////////////////////////////////
$(document).on("keyup", "#Row27 .Disper", function () {

    var Amount = $("#Row27 .Qty").val() * $("#Row27 .Price").val() - ($("#Row27 .Qty").val() * $("#Row27 .Price").val() * ($("#Row27 .Disper").val() / 100));
    var Net = $("#Row27 .Net").text()
    var DisValue = Net - Amount;

    $("#Row27 .DisValue").val(DisValue.toFixed(2));//قيمه الخصم نفسه
    $("#Row27 .Net").text(Amount.toFixed(2));//القيمه بعد الخصم

});
//////////////////////////////////////////////////////////////////////////////////////////////
$(document).on("keyup", "#Row28 .Disper", function () {

    var Amount = $("#Row28 .Qty").val() * $("#Row28 .Price").val() - ($("#Row28 .Qty").val() * $("#Row28 .Price").val() * ($("#Row28 .Disper").val() / 100));
    var Net = $("#Row28 .Net").text()
    var DisValue = Net - Amount;

    $("#Row28 .DisValue").val(DisValue.toFixed(2));//قيمه الخصم نفسه
    $("#Row28 .Net").text(Amount.toFixed(2));//القيمه بعد الخصم

});
//////////////////////////////////////////////////////////////////////////////////////////////
$(document).on("keyup", "#Row29 .Disper", function () {

    var Amount = $("#Row29 .Qty").val() * $("#Row29 .Price").val() - ($("#Row29 .Qty").val() * $("#Row29 .Price").val() * ($("#Row29 .Disper").val() / 100));
    var Net = $("#Row29 .Net").text()
    var DisValue = Net - Amount;

    $("#Row29 .DisValue").val(DisValue.toFixed(2));//قيمه الخصم نفسه
    $("#Row29 .Net").text(Amount.toFixed(2));//القيمه بعد الخصم

});
//////////////////////////////////////////////////////////////////////////////////////////////
$(document).on("keyup", "#Row30 .Disper", function () {

    var Amount = $("#Row30 .Qty").val() * $("#Row30 .Price").val() - ($("#Row30 .Qty").val() * $("#Row30 .Price").val() * ($("#Row30 .Disper").val() / 100));
    var Net = $("#Row30 .Net").text()
    var DisValue = Net - Amount;

    $("#Row30 .DisValue").val(DisValue.toFixed(2));//قيمه الخصم نفسه
    $("#Row30 .Net").text(Amount.toFixed(2));//القيمه بعد الخصم

});
//حساب نسبه الخصم عن طريق قيمه الخصم
$(document).on("keyup", "#DisValue", function () {

    var Amount = $("#Qty").val() * $("#Price").val();
    var DisValue = $("#DisValue").val();

    var DisPer = DisValue / Amount * 100;

    var DisValue = Amount - DisValue;

    $("#Disper").val(DisPer.toFixed(2));//قيمه الخصم نفسه
    $("#Net").text(DisValue.toFixed(2));//القيمه بعد الخصم 

});

////////////////////////////////////////////////////////////////////////////////////////////
$(document).on("keyup", "#Row1 .DisValue", function () {

    var Amount = $("#Row1 .Qty").val() * $("#Row1 .Price").val();
    var DisValue = $("#Row1 .DisValue").val();

    var DisPer = DisValue / Amount * 100;

    var DisValue = Amount - DisValue;

    $("#Row1 .Disper").val(DisPer.toFixed(2));//قيمه الخصم نفسه
    $("#Row1 .Net").text(DisValue.toFixed(2));//القيمه بعد الخصم 

});
////////////////////////////////////////////////////////////////////////////////////////////
$(document).on("keyup", "#Row2 .DisValue", function () {

    var Amount = $("#Row2 .Qty").val() * $("#Row2 .Price").val();
    var DisValue = $("#Row2 .DisValue").val();

    var DisPer = DisValue / Amount * 100;

    var DisValue = Amount - DisValue;

    $("#Row2 .Disper").val(DisPer.toFixed(2));//قيمه الخصم نفسه
    $("#Row2 .Net").text(DisValue.toFixed(2));//القيمه بعد الخصم

});
////////////////////////////////////////////////////////////////////////////////////////////
$(document).on("keyup", "#Row3 .DisValue", function () {

    var Amount = $("#Row3 .Qty").val() * $("#Row3 .Price").val();
    var DisValue = $("#Row3 .DisValue").val();

    var DisPer = DisValue / Amount * 100;

    var DisValue = Amount - DisValue;

    $("#Row3 .Disper").val(DisPer.toFixed(2));//قيمه الخصم نفسه
    $("#Row3 .Net").text(DisValue.toFixed(2));//القيمه بعد الخصم

});
////////////////////////////////////////////////////////////////////////////////////////////
$(document).on("keyup", "#Row4 .DisValue", function () {

    var Amount = $("#Row4 .Qty").val() * $("#Row4 .Price").val();
    var DisValue = $("#Row4 .DisValue").val();

    var DisPer = DisValue / Amount * 100;

    var DisValue = Amount - DisValue;

    $("#Row4 .Disper").val(DisPer.toFixed(2));//قيمه الخصم نفسه
    $("#Row4 .Net").text(DisValue.toFixed(2));//القيمه بعد الخصم

});
////////////////////////////////////////////////////////////////////////////////////////////
$(document).on("keyup", "#Row5 .DisValue", function () {

    var Amount = $("#Row5 .Qty").val() * $("#Row5 .Price").val();
    var DisValue = $("#Row5 .DisValue").val();

    var DisPer = DisValue / Amount * 100;

    var DisValue = Amount - DisValue;

    $("#Row5 .Disper").val(DisPer.toFixed(2));//قيمه الخصم نفسه
    $("#Row5 .Net").text(DisValue.toFixed(2));//القيمه بعد الخصم

});
////////////////////////////////////////////////////////////////////////////////////////////
$(document).on("keyup", "#Row6 .DisValue", function () {

    var Amount = $("#Row6 .Qty").val() * $("#Row6 .Price").val();
    var DisValue = $("#Row6 .DisValue").val();

    var DisPer = DisValue / Amount * 100;

    var DisValue = Amount - DisValue;

    $("#Row6 .Disper").val(DisPer.toFixed(2));//قيمه الخصم نفسه
    $("#Row6 .Net").text(DisValue.toFixed(2));//القيمه بعد الخصم

});
////////////////////////////////////////////////////////////////////////////////////////////
$(document).on("keyup", "#Row7 .DisValue", function () {

    var Amount = $("#Row7 .Qty").val() * $("#Row7 .Price").val();
    var DisValue = $("#Row7 .DisValue").val();

    var DisPer = DisValue / Amount * 100;

    var DisValue = Amount - DisValue;

    $("#Row7 .Disper").val(DisPer.toFixed(2));//قيمه الخصم نفسه
    $("#Row7 .Net").text(DisValue.toFixed(2));//القيمه بعد الخصم

});
////////////////////////////////////////////////////////////////////////////////////////////
$(document).on("keyup", "#Row8 .DisValue", function () {

    var Amount = $("#Row8 .Qty").val() * $("#Row8 .Price").val();
    var DisValue = $("#Row8 .DisValue").val();

    var DisPer = DisValue / Amount * 100;

    var DisValue = Amount - DisValue;

    $("#Row8 .Disper").val(DisPer.toFixed(2));//قيمه الخصم نفسه
    $("#Row8 .Net").text(DisValue.toFixed(2));//القيمه بعد الخصم

});
////////////////////////////////////////////////////////////////////////////////////////////
$(document).on("keyup", "#Row9 .DisValue", function () {

    var Amount = $("#Row9 .Qty").val() * $("#Row9 .Price").val();
    var DisValue = $("#Row9 .DisValue").val();

    var DisPer = DisValue / Amount * 100;

    var DisValue = Amount - DisValue;

    $("#Row9 .Disper").val(DisPer.toFixed(2));//قيمه الخصم نفسه
    $("#Row9 .Net").text(DisValue.toFixed(2));//القيمه بعد الخصم

});
////////////////////////////////////////////////////////////////////////////////////////////
$(document).on("keyup", "#Row10 .DisValue", function () {

    var Amount = $("#Row10 .Qty").val() * $("#Row10 .Price").val();
    var DisValue = $("#Row10 .DisValue").val();

    var DisPer = DisValue / Amount * 100;

    var DisValue = Amount - DisValue;

    $("#Row10 .Disper").val(DisPer.toFixed(2));//قيمه الخصم نفسه
    $("#Row10 .Net").text(DisValue.toFixed(2));//القيمه بعد الخصم

});
////////////////////////////////////////////////////////////////////////////////////////////
$(document).on("keyup", "#Row11 .DisValue", function () {

    var Amount = $("#Row11 .Qty").val() * $("#Row11 .Price").val();
    var DisValue = $("#Row11 .DisValue").val();

    var DisPer = DisValue / Amount * 100;

    var DisValue = Amount - DisValue;

    $("#Row11 .Disper").val(DisPer.toFixed(2));//قيمه الخصم نفسه
    $("#Row11 .Net").text(DisValue.toFixed(2));//القيمه بعد الخصم

});
////////////////////////////////////////////////////////////////////////////////////////////
$(document).on("keyup", "#Row12 .DisValue", function () {

    var Amount = $("#Row12 .Qty").val() * $("#Row12 .Price").val();
    var DisValue = $("#Row12 .DisValue").val();

    var DisPer = DisValue / Amount * 100;

    var DisValue = Amount - DisValue;

    $("#Row12 .Disper").val(DisPer.toFixed(2));//قيمه الخصم نفسه
    $("#Row12 .Net").text(DisValue.toFixed(2));//القيمه بعد الخصم

});
////////////////////////////////////////////////////////////////////////////////////////////
$(document).on("keyup", "#Row13 .DisValue", function () {

    var Amount = $("#Row13 .Qty").val() * $("#Row13 .Price").val();
    var DisValue = $("#Row13 .DisValue").val();

    var DisPer = DisValue / Amount * 100;

    var DisValue = Amount - DisValue;

    $("#Row13 .Disper").val(DisPer.toFixed(2));//قيمه الخصم نفسه
    $("#Row13 .Net").text(DisValue.toFixed(2));//القيمه بعد الخصم

});
////////////////////////////////////////////////////////////////////////////////////////////
$(document).on("keyup", "#Row14 .DisValue", function () {

    var Amount = $("#Row14 .Qty").val() * $("#Row14 .Price").val();
    var DisValue = $("#Row14 .DisValue").val();

    var DisPer = DisValue / Amount * 100;

    var DisValue = Amount - DisValue;

    $("#Row14 .Disper").val(DisPer.toFixed(2));//قيمه الخصم نفسه
    $("#Row14 .Net").text(DisValue.toFixed(2));//القيمه بعد الخصم

});

////////////////////////////////////////////////////////////////////////////////////////////
$(document).on("keyup", "#Row15 .DisValue", function () {

    var Amount = $("#Row15 .Qty").val() * $("#Row15 .Price").val();
    var DisValue = $("#Row15 .DisValue").val();

    var DisPer = DisValue / Amount * 100;

    var DisValue = Amount - DisValue;

    $("#Row15 .Disper").val(DisPer.toFixed(2));//قيمه الخصم نفسه
    $("#Row15 .Net").text(DisValue.toFixed(2));//القيمه بعد الخصم

});
////////////////////////////////////////////////////////////////////////////////////////////
$(document).on("keyup", "#Row16 .DisValue", function () {

    var Amount = $("#Row16 .Qty").val() * $("#Row16 .Price").val();
    var DisValue = $("#Row16 .DisValue").val();

    var DisPer = DisValue / Amount * 100;

    var DisValue = Amount - DisValue;

    $("#Row16 .Disper").val(DisPer.toFixed(2));//قيمه الخصم نفسه
    $("#Row16 .Net").text(DisValue.toFixed(2));//القيمه بعد الخصم

});
////////////////////////////////////////////////////////////////////////////////////////////
$(document).on("keyup", "#Row17 .DisValue", function () {

    var Amount = $("#Row17 .Qty").val() * $("#Row17 .Price").val();
    var DisValue = $("#Row17 .DisValue").val();

    var DisPer = DisValue / Amount * 100;

    var DisValue = Amount - DisValue;

    $("#Row17 .Disper").val(DisPer.toFixed(2));//قيمه الخصم نفسه
    $("#Row17 .Net").text(DisValue.toFixed(2));//القيمه بعد الخصم

});
////////////////////////////////////////////////////////////////////////////////////////////
$(document).on("keyup", "#Row18 .DisValue", function () {

    var Amount = $("#Row18 .Qty").val() * $("#Row18 .Price").val();
    var DisValue = $("#Row18 .DisValue").val();

    var DisPer = DisValue / Amount * 100;

    var DisValue = Amount - DisValue;

    $("#Row18 .Disper").val(DisPer.toFixed(2));//قيمه الخصم نفسه
    $("#Row18 .Net").text(DisValue.toFixed(2));//القيمه بعد الخصم

});
////////////////////////////////////////////////////////////////////////////////////////////
$(document).on("keyup", "#Row19 .DisValue", function () {

    var Amount = $("#Row19 .Qty").val() * $("#Row19 .Price").val();
    var DisValue = $("#Row19 .DisValue").val();

    var DisPer = DisValue / Amount * 100;

    var DisValue = Amount - DisValue;

    $("#Row19 .Disper").val(DisPer.toFixed(2));//قيمه الخصم نفسه
    $("#Row19 .Net").text(DisValue.toFixed(2));//القيمه بعد الخصم

});
////////////////////////////////////////////////////////////////////////////////////////////
$(document).on("keyup", "#Row20 .DisValue", function () {

    var Amount = $("#Row20 .Qty").val() * $("#Row20 .Price").val();
    var DisValue = $("#Row20 .DisValue").val();

    var DisPer = DisValue / Amount * 100;

    var DisValue = Amount - DisValue;

    $("#Row20 .Disper").val(DisPer.toFixed(2));//قيمه الخصم نفسه
    $("#Row20 .Net").text(DisValue.toFixed(2));//القيمه بعد الخصم

});

////////////////////////////////////////////////////////////////////////////////////////////
$(document).on("keyup", "#Row21 .DisValue", function () {

    var Amount = $("#Row21 .Qty").val() * $("#Row21 .Price").val();
    var DisValue = $("#Row21 .DisValue").val();

    var DisPer = DisValue / Amount * 100;

    var DisValue = Amount - DisValue;

    $("#Row21 .Disper").val(DisPer.toFixed(2));//قيمه الخصم نفسه
    $("#Row21 .Net").text(DisValue.toFixed(2));//القيمه بعد الخصم

});
////////////////////////////////////////////////////////////////////////////////////////////
$(document).on("keyup", "#Row22 .DisValue", function () {

    var Amount = $("#Row22 .Qty").val() * $("#Row22 .Price").val();
    var DisValue = $("#Row22 .DisValue").val();

    var DisPer = DisValue / Amount * 100;

    var DisValue = Amount - DisValue;

    $("#Row22 .Disper").val(DisPer.toFixed(2));//قيمه الخصم نفسه
    $("#Row22 .Net").text(DisValue.toFixed(2));//القيمه بعد الخصم

});
////////////////////////////////////////////////////////////////////////////////////////////
$(document).on("keyup", "#Row23 .DisValue", function () {

    var Amount = $("#Row23 .Qty").val() * $("#Row23 .Price").val();
    var DisValue = $("#Row23 .DisValue").val();

    var DisPer = DisValue / Amount * 100;

    var DisValue = Amount - DisValue;

    $("#Row23 .Disper").val(DisPer.toFixed(2));//قيمه الخصم نفسه
    $("#Row23 .Net").text(DisValue.toFixed(2));//القيمه بعد الخصم

});
////////////////////////////////////////////////////////////////////////////////////////////
$(document).on("keyup", "#Row24 .DisValue", function () {

    var Amount = $("#Row24 .Qty").val() * $("#Row24 .Price").val();
    var DisValue = $("#Row24 .DisValue").val();

    var DisPer = DisValue / Amount * 100;

    var DisValue = Amount - DisValue;

    $("#Row24 .Disper").val(DisPer.toFixed(2));//قيمه الخصم نفسه
    $("#Row24 .Net").text(DisValue.toFixed(2));//القيمه بعد الخصم

});
////////////////////////////////////////////////////////////////////////////////////////////
$(document).on("keyup", "#Row25 .DisValue", function () {

    var Amount = $("#Row25 .Qty").val() * $("#Row25 .Price").val();
    var DisValue = $("#Row25 .DisValue").val();

    var DisPer = DisValue / Amount * 100;

    var DisValue = Amount - DisValue;

    $("#Row25 .Disper").val(DisPer.toFixed(2));//قيمه الخصم نفسه
    $("#Row25 .Net").text(DisValue.toFixed(2));//القيمه بعد الخصم

});
////////////////////////////////////////////////////////////////////////////////////////////
$(document).on("keyup", "#Row26 .DisValue", function () {

    var Amount = $("#Row26 .Qty").val() * $("#Row26 .Price").val();
    var DisValue = $("#Row26 .DisValue").val();

    var DisPer = DisValue / Amount * 100;

    var DisValue = Amount - DisValue;

    $("#Row26 .Disper").val(DisPer.toFixed(2));//قيمه الخصم نفسه
    $("#Row26 .Net").text(DisValue.toFixed(2));//القيمه بعد الخصم

});
////////////////////////////////////////////////////////////////////////////////////////////
$(document).on("keyup", "#Row27 .DisValue", function () {

    var Amount = $("#Row27 .Qty").val() * $("#Row27 .Price").val();
    var DisValue = $("#Row27 .DisValue").val();

    var DisPer = DisValue / Amount * 100;

    var DisValue = Amount - DisValue;

    $("#Row27 .Disper").val(DisPer.toFixed(2));//قيمه الخصم نفسه
    $("#Row27 .Net").text(DisValue.toFixed(2));//القيمه بعد الخصم

});
////////////////////////////////////////////////////////////////////////////////////////////
$(document).on("keyup", "#Row28 .DisValue", function () {

    var Amount = $("#Row28 .Qty").val() * $("#Row28 .Price").val();
    var DisValue = $("#Row28 .DisValue").val();

    var DisPer = DisValue / Amount * 100;

    var DisValue = Amount - DisValue;

    $("#Row28 .Disper").val(DisPer.toFixed(2));//قيمه الخصم نفسه
    $("#Row28 .Net").text(DisValue.toFixed(2));//القيمه بعد الخصم

});
////////////////////////////////////////////////////////////////////////////////////////////
$(document).on("keyup", "#Row29 .DisValue", function () {

    var Amount = $("#Row29 .Qty").val() * $("#Row29 .Price").val();
    var DisValue = $("#Row29 .DisValue").val();

    var DisPer = DisValue / Amount * 100;

    var DisValue = Amount - DisValue;

    $("#Row29 .Disper").val(DisPer.toFixed(2));//قيمه الخصم نفسه
    $("#Row29 .Net").text(DisValue.toFixed(2));//القيمه بعد الخصم

});
////////////////////////////////////////////////////////////////////////////////////////////
$(document).on("keyup", "#Row30 .DisValue", function () {

    var Amount = $("#Row30 .Qty").val() * $("#Row30 .Price").val();
    var DisValue = $("#Row30 .DisValue").val();

    var DisPer = DisValue / Amount * 100;

    var DisValue = Amount - DisValue;

    $("#Row30 .Disper").val(DisPer.toFixed(2));//قيمه الخصم نفسه
    $("#Row30 .Net").text(DisValue.toFixed(2));//القيمه بعد الخصم

});
//  حساب نسبه الخصم للاجمالي الكلي للفاتورة
$(document).on("keyup", "#TotalDisper", function () {

    var Amount = $("#TotalBeforeDis").text() - ($("#TotalBeforeDis").text() * ($("#TotalDisper").val() / 100));
    var Net = $("#TotalBeforeDis").text()
    var DisValue = Net - Amount;

    $("#TotalDis").val(DisValue.toFixed(2));//قيمه الخصم نفسه
    $("#TotalNet").text(Amount);//القيمه بعد الخصم 

});
//  حساب نسبه الخصم عن طريق قيمه الخصم للاجمالي الكلي للفاتورة
$(document).on("keyup", "#TotalDis", function () {

    var Amount = $("#TotalBeforeDis").text();
    var Net = $("#TotalDis").val();
    var DisValuetotalper = Net / Amount * 100;
    var total = Amount - Net;
    $("#TotalDisper").val(DisValuetotalper);//قيمه الخصم نفسه
    $("#TotalNet").text(total.toFixed(2));//القيمه بعد الخصم 

});
// حساب اجمالي عدد الاصناف اجمالي الكميه قبل الخصم اجمالي قيمه الخصم اجمالي القيمه بعد الخصم




// Delete Tr from table
//لحذف صنف من الفاتوره قبل الحفظ
$("#OrdersItems").on("click", ".remove", function () {

    var Quat = $("#Qty").val();
    var Total = $("#Total").text();
    var Disper = $("#Disper").val();
    var DisValue = $("#DisValue").val();

    var ans = confirm("هل تريد الحذف بالفعل ؟!");
    if (ans) {
        var rowCount = $("#OrdersItems tbody tr").length;
       
        if (rowCount == 1) {
            toastr.error("يجب ان يكون هناك صنف واحد على الأقل")
        }
        else {
            $(this).parents("tr").remove();



            //حساب كل القيمه قبل الخصم لكل اصناف الفاتوره

            var $numoItem = $("#TbItem tr:gt(0)");
            $numoItem.each(function (index) {
                var $tblrow = $(this);
                if (!isNaN(Total)) {
                    var TotalQuat = 0;

                    $(".Total").each(function () {
                        var sval = parseInt($(this).text());
                        TotalQuat += isNaN(sval) ? 0 : sval;
                    });

                    $(".TotalBeforeDis").text(TotalQuat);
                }


            });




            ///////////////////////////////////////////////////////////////////

            //حساب نسبه الخصم في اجمالي الفاتوره كلها

            var $numoItem = $("#TbItem tr:gt(0)");
            $numoItem.each(function (index) {
                var $tblrow = $(this);
                if (!isNaN(Disper)) {
                    var TotalQuat = 0;

                    $(".Disper").each(function () {
                        var sval = parseInt($(this).val());
                        TotalQuat += isNaN(sval) ? 0 : sval;
                    });

                    $(".TotalDisper").text(TotalQuat);
                }


            });

            ///////////////////////////////////////////////


            ///////////////////////////////////////////////////////////////////

            //حساب قيمه الخصم علي مستوي الفاتوره

            var $numoItem = $("#TbItem tr:gt(0)");
            $numoItem.each(function (index) {
                var $tblrow = $(this);
                if (!isNaN(DisValue)) {
                    var TotalQuat = 0;

                    $(".DisValue").each(function () {
                        var sval = parseInt($(this).val());
                        TotalQuat += isNaN(sval) ? 0 : sval;
                    });

                    $(".TotalDis").text(TotalQuat);
                    $("#TotaleItemDis").val(TotalQuat);

                }


            });

            ///////////////////////////////////////////////


            ///////////////////////////////////////////////
            //حساب عدد الاصناف في الفاتوره 
            var rowCount = $("#OrdersItems tbody tr:not(:nth-child(1))").length;

            $("#RowsCount").val(rowCount);

            //////////////////////////////////////////////////////////////////////////////

            //حساب اجمالي الصافي للفاتوره

            var Net = parseFloat($(".TotalBeforeDis").text()) - parseFloat($("#TotalDis").text());
            $("#TotalNet").text(Net.toFixed(2));

        }
    }
});

//$("#OrdersItems").on("click", ".remove", function () {

//    var Total = $("#Debitt").val();
//    var $numoItem = $("#TbItem tr:gt(0)");
//    var ans = confirm("هل تريد الحذف بالفعل ؟!");
//    if (ans) {
//        $(this).parents("tr").remove();
//    }
//    $numoItem.each(function (index) {
//        var $tblrow = $(this);
//        if (!isNaN(Total)) {
//            var TotalQuat = 0;

//            $(".Debitt").each(function () {
//                var sval = parseInt($(this).val());
//                TotalQuat += isNaN(sval) ? 0 : sval;
//            });

//            /* $(".TotalDis").text(TotalQuat);*/
//            $("#Deipt").val(TotalQuat);
//        }


//    });
//});

$("#add").click(function () {
    var isValid = true;
    //$('#OrdersItems tbody tr:not(:nth-child(1)) .ItemListPopUp').each(function () {

    //    var OldItem = $(this).val();
    //    var NewItem = $("#ItemListPopUp").val();
    //    if (NewItem == OldItem) {
    //        toastr.warning('الصنف موجود بالفعل في الفاتورة')
    //        isValid = false;
    //    }
    //});
 

    if (isValid) {

        var Quat = $("#Qty").val();
        var Total = $("#Total").text();
        var Disper = $("#Disper").val();
        var DisValue = $("#DisValue").val();









        //////////////////////////////////////////////////////////////////////////



        ///////////////////////////////////////////////////////////////////

        //get all toatal of total before Dis Osama Sadek
        //حساب كل القيمه قبل الخصم لكل اصناف الفاتوره

        var $numoItem = $("#TbItem tr:gt(0)");
        $numoItem.each(function (index) {
            var $tblrow = $(this);
            if (!isNaN(Total)) {
                var TotalQuat = 0;

                $(".Total").each(function () {
                    var sval = parseInt($(this).text());
                    TotalQuat += isNaN(sval) ? 0 : sval;
                });
                TotalQuat += ($("#DirectExpenses1").val()|0) + ($("#DirectExpenses2").val()|0);
                $(".TotalBeforeDis").text(TotalQuat);
            }


        });

        var Total1 = $("#Debitt").val();
        var $numoItem = $("#TbItem tr:gt(0)");
        $numoItem.each(function (index) {
            var $tblrow = $(this);
            if (!isNaN(Total1)) {
                var TotalQuat = 0;

                $(".Debitt").each(function () {
                    var sval = parseInt($(this).val());


                    TotalQuat += (isNaN(sval) ? 0 : sval);
                
                });

                /* $(".TotalDis").text(TotalQuat);*/
                $("#Deipt").val(TotalQuat);

            }


        });

        var Total2 = $("#Creditt").val();
        var $numoItem = $("#TbItem tr:gt(0)");
        $numoItem.each(function (index) {
            var $tblrow = $(this);
            if (!isNaN(Total2)) {
                var TotalQuat = 0;

                $(".Creditt").each(function () {
                    var sval = parseInt($(this).val());


                    TotalQuat += (isNaN(sval) ? 0 : sval);

                });

                /* $(".TotalDis").text(TotalQuat);*/
                $("#Credit").val(TotalQuat);

            }

            var CreditVal = $("#Credit").val();
            var DepitVal = $("#Deipt").val();
            var diffrence = CreditVal - DepitVal;
            $("#diff").val(diffrence)

        });


        ///////////////////////////////////////////////////////////////////

        //حساب نسبه الخصم في اجمالي الفاتوره كلها

        var $numoItem = $("#TbItem tr:gt(0)");
        $numoItem.each(function (index) {
            var $tblrow = $(this);
            if (!isNaN(Disper)) {
                var TotalQuat = 0;

                $(".Disper").each(function () {
                    var sval = parseInt($(this).val());
                    TotalQuat += isNaN(sval) ? 0 : sval;
                });

                $(".TotalDisper").text(TotalQuat);
            }


        });

        ///////////////////////////////////////////////


        ///////////////////////////////////////////////////////////////////

        //حساب قيمه الخصم علي مستوي الفاتوره

        var $numoItem = $("#TbItem tr:gt(0)");
        $numoItem.each(function (index) {
            var $tblrow = $(this);
            if (!isNaN(DisValue)) {
                var TotalQuat = 0;

                $(".DisValue").each(function () {
                    var sval = parseInt($(this).val());
                    TotalQuat += isNaN(sval) ? 0 : sval;
                });

                $(".TotalDis").text(TotalQuat);
                $("#TotaleItemDis").val(TotalQuat);

            }


        });

        ///////////////////////////////////////////////


        ///////////////////////////////////////////////
        //حساب عدد الاصناف في الفاتوره 
        var rowCount = $("#OrdersItems tr").length;

        $("#RowsCount").val(rowCount);

        //////////////////////////////////////////////////////////////////////////////

        //حساب اجمالي الصافي للفاتوره

        var Net = parseFloat($(".TotalBeforeDis").text()) - parseFloat($("#TotalDis").text());
        $("#TotalNet").text(Net.toFixed(2));
        ////////////////////////////////////////////////////////////////////////////


        //حساب المتبقي بعد الدفع

        $(document).on("change keyup blur", "#Payed", function () {
            var TotalNet = $('#TotalNet').text();
            var Payed = $('#Payed').val();

            var mult = Payed - TotalNet;
            var doubleVal = (mult).toFixed(2);

            $('#Residual').text(doubleVal);



        });


    }

});
//////////////////////////////////////
$("#addRowJournals").click(function () {
    var isValid = true;
    //$('#OrdersItems tbody tr:not(:nth-child(1)) .ItemListPopUp').each(function () {

    //    var OldItem = $(this).val();
    //    var NewItem = $("#ItemListPopUp").val();
    //    if (NewItem == OldItem) {
    //        toastr.warning('الصنف موجود بالفعل في الفاتورة')
    //        isValid = false;
    //    }
    //});


    if (isValid) {

        var Quat = $("#Qty").val();
        var Total = $("#Total").text();
        var Disper = $("#Disper").val();
        var DisValue = $("#DisValue").val();









        //////////////////////////////////////////////////////////////////////////



        ///////////////////////////////////////////////////////////////////

        //get all toatal of total before Dis Osama Sadek
        //حساب كل القيمه قبل الخصم لكل اصناف الفاتوره

        var $numoItem = $("#TbItem tr:gt(0)");
        $numoItem.each(function (index) {
            var $tblrow = $(this);
            if (!isNaN(Total)) {
                var TotalQuat = 0;

                $(".Total").each(function () {
                    var sval = parseInt($(this).text());
                    TotalQuat += isNaN(sval) ? 0 : sval;
                });
                TotalQuat += ($("#DirectExpenses1").val() | 0) + ($("#DirectExpenses2").val() | 0);
                $(".TotalBeforeDis").text(TotalQuat);
            }


        });

        var Total1 = $("#Debitt").val();
        var $numoItem = $("#TbItem tr:gt(0)");
        $numoItem.each(function (index) {
            var $tblrow = $(this);
            if (!isNaN(Total1)) {
                var TotalQuat = 0;

                $(".Debitt").each(function () {
                    var sval = parseInt($(this).val());


                    TotalQuat += (isNaN(sval) ? 0 : sval);

                });

                /* $(".TotalDis").text(TotalQuat);*/
                $("#Deipt").val(TotalQuat);

            }


        });

        var Total2 = $("#Creditt").val();
        var $numoItem = $("#TbItem tr:gt(0)");
        $numoItem.each(function (index) {
            var $tblrow = $(this);
            if (!isNaN(Total2)) {
                var TotalQuat = 0;

                $(".Creditt").each(function () {
                    var sval = parseInt($(this).val());


                    TotalQuat += (isNaN(sval) ? 0 : sval);

                });

                /* $(".TotalDis").text(TotalQuat);*/
                $("#Credit").val(TotalQuat);

            }

            var CreditVal = $("#Credit").val();
            var DepitVal = $("#Deipt").val();
            var diffrence = CreditVal - DepitVal;
            $("#diff").val(diffrence)

        });


        ///////////////////////////////////////////////////////////////////

        //حساب نسبه الخصم في اجمالي الفاتوره كلها

        var $numoItem = $("#TbItem tr:gt(0)");
        $numoItem.each(function (index) {
            var $tblrow = $(this);
            if (!isNaN(Disper)) {
                var TotalQuat = 0;

                $(".Disper").each(function () {
                    var sval = parseInt($(this).val());
                    TotalQuat += isNaN(sval) ? 0 : sval;
                });

                $(".TotalDisper").text(TotalQuat);
            }


        });

        ///////////////////////////////////////////////


        ///////////////////////////////////////////////////////////////////

        //حساب قيمه الخصم علي مستوي الفاتوره

        var $numoItem = $("#TbItem tr:gt(0)");
        $numoItem.each(function (index) {
            var $tblrow = $(this);
            if (!isNaN(DisValue)) {
                var TotalQuat = 0;

                $(".DisValue").each(function () {
                    var sval = parseInt($(this).val());
                    TotalQuat += isNaN(sval) ? 0 : sval;
                });

                $(".TotalDis").text(TotalQuat);
                $("#TotaleItemDis").val(TotalQuat);

            }


        });

        ///////////////////////////////////////////////


        ///////////////////////////////////////////////
        //حساب عدد الاصناف في الفاتوره 
        var rowCount = $("#OrdersItems tr").length;

        $("#RowsCount").val(rowCount);

        //////////////////////////////////////////////////////////////////////////////

        //حساب اجمالي الصافي للفاتوره

        var Net = parseFloat($(".TotalBeforeDis").text()) - parseFloat($("#TotalDis").text());
        $("#TotalNet").text(Net.toFixed(2));
        ////////////////////////////////////////////////////////////////////////////


        //حساب المتبقي بعد الدفع

        $(document).on("change keyup blur", "#Payed", function () {
            var TotalNet = $('#TotalNet').text();
            var Payed = $('#Payed').val();

            var mult = Payed - TotalNet;
            var doubleVal = (mult).toFixed(2);

            $('#Residual').text(doubleVal);



        });


    }

});
//////////////////////////////////////
$("#addRowPur").click(function () {
    var isValid = true;
    //$('#OrdersItems tbody tr:not(:nth-child(1)) .ItemListPopUp').each(function () {

    //    var OldItem = $(this).val();
    //    var NewItem = $("#ItemListPopUp").val();
    //    if (NewItem == OldItem) {
    //        toastr.warning('الصنف موجود بالفعل في الفاتورة')
    //        isValid = false;
    //    }
    //});


    if (isValid) {

        var Quat = $("#Qty").val();
        var Total = $("#Total").text();
        var Disper = $("#Disper").val();
        var DisValue = $("#DisValue").val();









        //////////////////////////////////////////////////////////////////////////



        ///////////////////////////////////////////////////////////////////

        //get all toatal of total before Dis Osama Sadek
        //حساب كل القيمه قبل الخصم لكل اصناف الفاتوره

        var $numoItem = $("#TbItem tr:gt(0)");
        $numoItem.each(function (index) {
            var $tblrow = $(this);
            if (!isNaN(Total)) {
                var TotalQuat = 0;

                $(".Total").each(function () {
                    var sval = parseInt($(this).text());
                    TotalQuat += isNaN(sval) ? 0 : sval;
                });
                TotalQuat += ($("#DirectExpenses1").val() | 0) + ($("#DirectExpenses2").val() | 0);
                $(".TotalBeforeDis").text(TotalQuat);
            }


        });

        var Total1 = $("#Debitt").val();
        var $numoItem = $("#TbItem tr:gt(0)");
        $numoItem.each(function (index) {
            var $tblrow = $(this);
            if (!isNaN(Total1)) {
                var TotalQuat = 0;

                $(".Debitt").each(function () {
                    var sval = parseInt($(this).val());


                    TotalQuat += (isNaN(sval) ? 0 : sval);

                });

                /* $(".TotalDis").text(TotalQuat);*/
                $("#Deipt").val(TotalQuat);

            }


        });

        var Total2 = $("#Creditt").val();
        var $numoItem = $("#TbItem tr:gt(0)");
        $numoItem.each(function (index) {
            var $tblrow = $(this);
            if (!isNaN(Total2)) {
                var TotalQuat = 0;

                $(".Creditt").each(function () {
                    var sval = parseInt($(this).val());


                    TotalQuat += (isNaN(sval) ? 0 : sval);

                });

                /* $(".TotalDis").text(TotalQuat);*/
                $("#Credit").val(TotalQuat);

            }

            var CreditVal = $("#Credit").val();
            var DepitVal = $("#Deipt").val();
            var diffrence = CreditVal - DepitVal;
            $("#diff").val(diffrence)

        });


        ///////////////////////////////////////////////////////////////////

        //حساب نسبه الخصم في اجمالي الفاتوره كلها

        var $numoItem = $("#TbItem tr:gt(0)");
        $numoItem.each(function (index) {
            var $tblrow = $(this);
            if (!isNaN(Disper)) {
                var TotalQuat = 0;

                $(".Disper").each(function () {
                    var sval = parseInt($(this).val());
                    TotalQuat += isNaN(sval) ? 0 : sval;
                });

                $(".TotalDisper").text(TotalQuat);
            }


        });

        ///////////////////////////////////////////////


        ///////////////////////////////////////////////////////////////////

        //حساب قيمه الخصم علي مستوي الفاتوره

        var $numoItem = $("#TbItem tr:gt(0)");
        $numoItem.each(function (index) {
            var $tblrow = $(this);
            if (!isNaN(DisValue)) {
                var TotalQuat = 0;

                $(".DisValue").each(function () {
                    var sval = parseInt($(this).val());
                    TotalQuat += isNaN(sval) ? 0 : sval;
                });

                $(".TotalDis").text(TotalQuat);
                $("#TotaleItemDis").val(TotalQuat);

            }


        });

        ///////////////////////////////////////////////


        ///////////////////////////////////////////////
        //حساب عدد الاصناف في الفاتوره 
        var rowCount = $("#OrdersItems tr").length;

        $("#RowsCount").val(rowCount);

        //////////////////////////////////////////////////////////////////////////////

        //حساب اجمالي الصافي للفاتوره

        var Net = parseFloat($(".TotalBeforeDis").text()) - parseFloat($("#TotalDis").text());
        $("#TotalNet").text(Net.toFixed(2));
        ////////////////////////////////////////////////////////////////////////////


        //حساب المتبقي بعد الدفع

        $(document).on("change keyup blur", "#Payed", function () {
            var TotalNet = $('#TotalNet').text();
            var Payed = $('#Payed').val();

            var mult = Payed - TotalNet;
            var doubleVal = (mult).toFixed(2);

            $('#Residual').text(doubleVal);



        });


    }

});
///////////////////////////////////////
$("#addRowRec").click(function () {
    var isValid = true;
    //$('#OrdersItems tbody tr:not(:nth-child(1)) .ItemListPopUp').each(function () {

    //    var OldItem = $(this).val();
    //    var NewItem = $("#ItemListPopUp").val();
    //    if (NewItem == OldItem) {
    //        toastr.warning('الصنف موجود بالفعل في الفاتورة')
    //        isValid = false;
    //    }
    //});


    if (isValid) {

        var Quat = $("#Qty").val();
        var Total = $("#Total").text();
        var Disper = $("#Disper").val();
        var DisValue = $("#DisValue").val();









        //////////////////////////////////////////////////////////////////////////



        ///////////////////////////////////////////////////////////////////

        //get all toatal of total before Dis Osama Sadek
        //حساب كل القيمه قبل الخصم لكل اصناف الفاتوره

        var $numoItem = $("#TbItem tr:gt(0)");
        $numoItem.each(function (index) {
            var $tblrow = $(this);
            if (!isNaN(Total)) {
                var TotalQuat = 0;

                $(".Total").each(function () {
                    var sval = parseInt($(this).text());
                    TotalQuat += isNaN(sval) ? 0 : sval;
                });
                TotalQuat += ($("#DirectExpenses1").val() | 0) + ($("#DirectExpenses2").val() | 0);
                $(".TotalBeforeDis").text(TotalQuat);
            }


        });

        var Total1 = $("#Debitt").val();
        var $numoItem = $("#TbItem tr:gt(0)");
        $numoItem.each(function (index) {
            var $tblrow = $(this);
            if (!isNaN(Total1)) {
                var TotalQuat = 0;

                $(".Debitt").each(function () {
                    var sval = parseInt($(this).val());


                    TotalQuat += (isNaN(sval) ? 0 : sval);

                });

                /* $(".TotalDis").text(TotalQuat);*/
                $("#Deipt").val(TotalQuat);

            }


        });

        var Total2 = $("#Creditt").val();
        var $numoItem = $("#TbItem tr:gt(0)");
        $numoItem.each(function (index) {
            var $tblrow = $(this);
            if (!isNaN(Total2)) {
                var TotalQuat = 0;

                $(".Creditt").each(function () {
                    var sval = parseInt($(this).val());


                    TotalQuat += (isNaN(sval) ? 0 : sval);

                });

                /* $(".TotalDis").text(TotalQuat);*/
                $("#Credit").val(TotalQuat);

            }

            var CreditVal = $("#Credit").val();
            var DepitVal = $("#Deipt").val();
            var diffrence = CreditVal - DepitVal;
            $("#diff").val(diffrence)

        });


        ///////////////////////////////////////////////////////////////////

        //حساب نسبه الخصم في اجمالي الفاتوره كلها

        var $numoItem = $("#TbItem tr:gt(0)");
        $numoItem.each(function (index) {
            var $tblrow = $(this);
            if (!isNaN(Disper)) {
                var TotalQuat = 0;

                $(".Disper").each(function () {
                    var sval = parseInt($(this).val());
                    TotalQuat += isNaN(sval) ? 0 : sval;
                });

                $(".TotalDisper").text(TotalQuat);
            }


        });

        ///////////////////////////////////////////////


        ///////////////////////////////////////////////////////////////////

        //حساب قيمه الخصم علي مستوي الفاتوره

        var $numoItem = $("#TbItem tr:gt(0)");
        $numoItem.each(function (index) {
            var $tblrow = $(this);
            if (!isNaN(DisValue)) {
                var TotalQuat = 0;

                $(".DisValue").each(function () {
                    var sval = parseInt($(this).val());
                    TotalQuat += isNaN(sval) ? 0 : sval;
                });

                $(".TotalDis").text(TotalQuat);
                $("#TotaleItemDis").val(TotalQuat);

            }


        });

        ///////////////////////////////////////////////


        ///////////////////////////////////////////////
        //حساب عدد الاصناف في الفاتوره 
        var rowCount = $("#OrdersItems tr").length;

        $("#RowsCount").val(rowCount);

        //////////////////////////////////////////////////////////////////////////////

        //حساب اجمالي الصافي للفاتوره

        var Net = parseFloat($(".TotalBeforeDis").text()) - parseFloat($("#TotalDis").text());
        $("#TotalNet").text(Net.toFixed(2));
        ////////////////////////////////////////////////////////////////////////////


        //حساب المتبقي بعد الدفع

        $(document).on("change keyup blur", "#Payed", function () {
            var TotalNet = $('#TotalNet').text();
            var Payed = $('#Payed').val();

            var mult = Payed - TotalNet;
            var doubleVal = (mult).toFixed(2);

            $('#Residual').text(doubleVal);



        });


    }

});
///////////////////////////////////////
$("#addRowSales").click(function () {
    var isValid = true;
    //$('#OrdersItems tbody tr:not(:nth-child(1)) .ItemListPopUp').each(function () {

    //    var OldItem = $(this).val();
    //    var NewItem = $("#ItemListPopUp").val();
    //    if (NewItem == OldItem) {
    //        toastr.warning('الصنف موجود بالفعل في الفاتورة')
    //        isValid = false;
    //    }
    //});


    if (isValid) {

        var Quat = $("#Qty").val();
        var Total = $("#Total").text();
        var Disper = $("#Disper").val();
        var DisValue = $("#DisValue").val();









        //////////////////////////////////////////////////////////////////////////



        ///////////////////////////////////////////////////////////////////

        //get all toatal of total before Dis Osama Sadek
        //حساب كل القيمه قبل الخصم لكل اصناف الفاتوره

        var $numoItem = $("#TbItem tr:gt(0)");
        $numoItem.each(function (index) {
            var $tblrow = $(this);
            if (!isNaN(Total)) {
                var TotalQuat = 0;

                $(".Total").each(function () {
                    var sval = parseInt($(this).text());
                    TotalQuat += isNaN(sval) ? 0 : sval;
                });
                TotalQuat += ($("#DirectExpenses1").val() | 0) + ($("#DirectExpenses2").val() | 0);
                $(".TotalBeforeDis").text(TotalQuat);
            }


        });

        var Total1 = $("#Debitt").val();
        var $numoItem = $("#TbItem tr:gt(0)");
        $numoItem.each(function (index) {
            var $tblrow = $(this);
            if (!isNaN(Total1)) {
                var TotalQuat = 0;

                $(".Debitt").each(function () {
                    var sval = parseInt($(this).val());


                    TotalQuat += (isNaN(sval) ? 0 : sval);

                });

                /* $(".TotalDis").text(TotalQuat);*/
                $("#Deipt").val(TotalQuat);

            }


        });

        var Total2 = $("#Creditt").val();
        var $numoItem = $("#TbItem tr:gt(0)");
        $numoItem.each(function (index) {
            var $tblrow = $(this);
            if (!isNaN(Total2)) {
                var TotalQuat = 0;

                $(".Creditt").each(function () {
                    var sval = parseInt($(this).val());


                    TotalQuat += (isNaN(sval) ? 0 : sval);

                });

                /* $(".TotalDis").text(TotalQuat);*/
                $("#Credit").val(TotalQuat);

            }

            var CreditVal = $("#Credit").val();
            var DepitVal = $("#Deipt").val();
            var diffrence = CreditVal - DepitVal;
            $("#diff").val(diffrence)

        });


        ///////////////////////////////////////////////////////////////////

        //حساب نسبه الخصم في اجمالي الفاتوره كلها

        var $numoItem = $("#TbItem tr:gt(0)");
        $numoItem.each(function (index) {
            var $tblrow = $(this);
            if (!isNaN(Disper)) {
                var TotalQuat = 0;

                $(".Disper").each(function () {
                    var sval = parseInt($(this).val());
                    TotalQuat += isNaN(sval) ? 0 : sval;
                });

                $(".TotalDisper").text(TotalQuat);
            }


        });

        ///////////////////////////////////////////////


        ///////////////////////////////////////////////////////////////////

        //حساب قيمه الخصم علي مستوي الفاتوره

        var $numoItem = $("#TbItem tr:gt(0)");
        $numoItem.each(function (index) {
            var $tblrow = $(this);
            if (!isNaN(DisValue)) {
                var TotalQuat = 0;

                $(".DisValue").each(function () {
                    var sval = parseInt($(this).val());
                    TotalQuat += isNaN(sval) ? 0 : sval;
                });

                $(".TotalDis").text(TotalQuat);
                $("#TotaleItemDis").val(TotalQuat);

            }


        });

        ///////////////////////////////////////////////


        ///////////////////////////////////////////////
        //حساب عدد الاصناف في الفاتوره 
        var rowCount = $("#OrdersItems tr").length;

        $("#RowsCount").val(rowCount);

        //////////////////////////////////////////////////////////////////////////////

        //حساب اجمالي الصافي للفاتوره

        var Net = parseFloat($(".TotalBeforeDis").text()) - parseFloat($("#TotalDis").text());
        $("#TotalNet").text(Net.toFixed(2));
        ////////////////////////////////////////////////////////////////////////////


        //حساب المتبقي بعد الدفع

        $(document).on("change keyup blur", "#Payed", function () {
            var TotalNet = $('#TotalNet').text();
            var Payed = $('#Payed').val();

            var mult = Payed - TotalNet;
            var doubleVal = (mult).toFixed(2);

            $('#Residual').text(doubleVal);



        });


    }

});



$("#addEdit").click(function () {
    var isValid = true;
    //$('#OrdersItems tbody tr:not(:nth-child(1)) .ItemListPopUp').each(function () {

    //    var OldItem = $(this).val();
    //    var NewItem = $("#ItemListPopUp").val();
    //    if (NewItem == OldItem) {
    //        toastr.warning('الصنف موجود بالفعل في الفاتورة')
    //        isValid = false;
    //    }
    //});


    if (isValid) {

        var Quat = $("#Qty").val();
        var Total = $("#Total").text();
        var Disper = $("#Disper").val();
        var DisValue = $("#DisValue").val();









        //////////////////////////////////////////////////////////////////////////



        ///////////////////////////////////////////////////////////////////

        //get all toatal of total before Dis Osama Sadek
        //حساب كل القيمه قبل الخصم لكل اصناف الفاتوره

        var $numoItem = $("#TbItem tr:gt(0)");
        $numoItem.each(function (index) {
            var $tblrow = $(this);
            if (!isNaN(Total)) {
                var TotalQuat = 0;

                $(".Total").each(function () {
                    var sval = parseInt($(this).text());
                    TotalQuat += isNaN(sval) ? 0 : sval;
                });
                TotalQuat += ($("#DirectExpenses1").val() | 0) + ($("#DirectExpenses2").val() | 0);
                $(".TotalBeforeDis").text(TotalQuat);
            }


        });

        var Total1 = $("#Debitt").val();
        var $numoItem = $("#TbItem tr:gt(0)");
        $numoItem.each(function (index) {
            var $tblrow = $(this);
            if (!isNaN(Total1)) {
                var TotalQuat = 0;

                $(".Debitt").each(function () {
                    var sval = parseInt($(this).val());


                    TotalQuat += (isNaN(sval) ? 0 : sval);

                });

                /* $(".TotalDis").text(TotalQuat);*/
                $("#Deipt").val(TotalQuat);

            }


        });

        var Total2 = $("#Creditt").val();
        var $numoItem = $("#TbItem tr:gt(0)");
        $numoItem.each(function (index) {
            var $tblrow = $(this);
            if (!isNaN(Total2)) {
                var TotalQuat = 0;

                $(".Creditt").each(function () {
                    var sval = parseInt($(this).val());


                    TotalQuat += (isNaN(sval) ? 0 : sval);

                });

                /* $(".TotalDis").text(TotalQuat);*/
                $("#Credit").val(TotalQuat);

            }

            var CreditVal = $("#Credit").val();
            var DepitVal = $("#Deipt").val();
            var diffrence = CreditVal - DepitVal;
            $("#diff").val(diffrence)

        });


        ///////////////////////////////////////////////////////////////////

        //حساب نسبه الخصم في اجمالي الفاتوره كلها

        var $numoItem = $("#TbItem tr:gt(0)");
        $numoItem.each(function (index) {
            var $tblrow = $(this);
            if (!isNaN(Disper)) {
                var TotalQuat = 0;

                $(".Disper").each(function () {
                    var sval = parseInt($(this).val());
                    TotalQuat += isNaN(sval) ? 0 : sval;
                });

                $(".TotalDisper").text(TotalQuat);
            }


        });

        ///////////////////////////////////////////////


        ///////////////////////////////////////////////////////////////////

        //حساب قيمه الخصم علي مستوي الفاتوره

        var $numoItem = $("#TbItem tr:gt(0)");
        $numoItem.each(function (index) {
            var $tblrow = $(this);
            if (!isNaN(DisValue)) {
                var TotalQuat = 0;

                $(".DisValue").each(function () {
                    var sval = parseInt($(this).val());
                    TotalQuat += isNaN(sval) ? 0 : sval;
                });

                $(".TotalDis").text(TotalQuat);
                $("#TotaleItemDis").val(TotalQuat);

            }


        });

        ///////////////////////////////////////////////


        ///////////////////////////////////////////////
        //حساب عدد الاصناف في الفاتوره 
        var rowCount = $("#OrdersItems tr").length;

        $("#RowsCount").val(rowCount);

        //////////////////////////////////////////////////////////////////////////////

        //حساب اجمالي الصافي للفاتوره

        var Net = parseFloat($(".TotalBeforeDis").text()) - parseFloat($("#TotalDis").text());
        $("#TotalNet").text(Net.toFixed(2));
        ////////////////////////////////////////////////////////////////////////////


        //حساب المتبقي بعد الدفع

        $(document).on("change keyup blur", "#Payed", function () {
            var TotalNet = $('#TotalNet').text();
            var Payed = $('#Payed').val();

            var mult = Payed - TotalNet;
            var doubleVal = (mult).toFixed(2);

            $('#Residual').text(doubleVal);



        });


    }

});
